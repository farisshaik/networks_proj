# Claude Instructions

## Implementation Decisions

Before making any non-trivial interpretation choices — such as how a feature should behave, what approach to use when requirements are ambiguous, or any design decision that isn't explicitly specified — stop and ask first. Briefly explain the options and tradeoffs, then let the user decide before writing any code.

Do not assume a convenient or "reasonable" default and implement it silently.
